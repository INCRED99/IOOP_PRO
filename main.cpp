// main.cpp
#include "Game.h"
#include <iostream>

int main() {
    // The Game object manages the entire game state, map, and player.
    Game myGame; 
    
    // Start the game loop
    myGame.startLoop();
    
    return 0;
}